using UnityEngine;

/// <summary>
/// PieceTestRunner — desactivado. El TurnManager maneja todo el input.
/// </summary>
public class PieceTestRunner : MonoBehaviour
{
    // Vacío intencionalmente. 
    // El testing se hace a través del TurnManager con ESPACIO y teclas 1-4.
    public PieceController piece;   // mantenido para no romper referencias en el Inspector
}
