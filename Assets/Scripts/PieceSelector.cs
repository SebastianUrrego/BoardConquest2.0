using UnityEngine;

/// <summary>
/// PieceSelector — componente de marcador en la ficha.
/// La selección ahora es por teclado (teclas 1-4) gestionada por TurnManager.
/// Este script existe para compatibilidad y para recibir el resaltado visual
/// desde PieceHighlight.
/// </summary>
public class PieceSelector : MonoBehaviour
{
    // Vacío intencionalmente: selección manejada por TurnManager via teclado.
}
